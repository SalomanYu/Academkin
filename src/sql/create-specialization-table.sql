CREATE TABLE specializations (
    vuz_fullname TEXT NOT NULL,
    id VARCHAR(6) NOT NULL,
    fullname TEXT NOT NULL,
    form_education VARCHAR(100),
    duration VARCHAR(50),
    preparation_level VARCHAR(100),
    qualification TEXT
);